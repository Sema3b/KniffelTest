public class ScoreCalculator
{
    // Methode zur Berechnung der Punktzahl für eine gegebene Kategorie
    public int CalculateScore(string category, int[] diceValues)
    {
        return 0;
    }

    // Hilfsmethode zur Berechnung der Summe einer bestimmten Zahl
    private int SumOfSpecificNumber(int number, int[] diceValues)
    {
        return 0;
    }

    // Hilfsmethode zur Berechnung der Summe für n-of-a-kind
    private int SumForNOfAKind(int n, int[] diceValues)
    {
        return 0;
    }

    // Hilfsmethode zur Berechnung der Punktzahl für ein Full House
    private int FullHouseScore(int[] diceValues)
    {
        return 0;
    }

    // Hilfsmethode zur Berechnung der Punktzahl für eine Kleine Straße
    private int SmallStraightScore(int[] diceValues)
    {
        return 0;
    }

    // Hilfsmethode zur Berechnung der Punktzahl für eine Große Straße
    private int LargeStraightScore(int[] diceValues)
    {
        return 0;
    }

    // Hilfsmethode zur Berechnung der Punktzahl für ein Kniffel (5 gleiche Zahlen)
    private int YahtzeeScore(int[] diceValues)
    {
        return 0;
    }

    // Hilfsmethode zur Überprüfung, ob eine gegebene Anzahl von aufeinanderfolgenden Werten vorhanden ist
    private bool IsConsecutive(int[] values, int length)
    {
        return false;
    }
}
